You are running in a remote sandbox environment via Claude Code Web.

- The `bun` runtime is available for running scripts and tests
- Use `bunx biome check --write .` to format and lint code before committing
- Run tests with `bun test` to verify changes
