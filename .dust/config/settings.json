{
  "dustCommand": "bin/dust",
  "checks": [
    {
      "name": "disallowed files",
      "command": "bash scripts/lint/disallowed-files.sh",
      "hints": [
        "Check scripts/lint/disallowed-files.sh for the list of disallowed files"
      ]
    },
    {
      "name": "lint (oxlint)",
      "command": "bunx oxlint .",
      "hints": [
        "Run `bunx oxlint .` to reproduce lint diagnostics locally",
        "Linting uses OXC defaults rather than Biome rule-by-rule parity"
      ]
    },
    {
      "name": "format (oxfmt)",
      "command": "bunx oxfmt --check --config .oxfmtrc.json \"lib/**/*.ts\" \"scripts/**/*.ts\" \"system-tests/**/*.ts\" \"evals/**/*.ts\" \"*.ts\" \"*.json\" \"!package.json\" && bun run scripts/lint/check-package-json-format.ts",
      "hints": [
        "Run `bunx oxfmt --config .oxfmtrc.json <paths>` to apply OXC formatting",
        "Run `bun run scripts/lint/check-package-json-format.ts --write` to restore package.json key ordering and formatting"
      ]
    },
    {
      "name": "lint (custom policy checks)",
      "command": "bun run scripts/lint/policy-checks.ts",
      "hints": [
        "This check enforces repository-owned policies that replaced Biome GritQL plugins",
        "See lib/lint/policy-checker.ts for rule definitions and messages"
      ]
    },
    {
      "name": "build",
      "command": "bun run build",
      "hints": ["Check for TypeScript errors in the files you modified"]
    },
    {
      "name": "unused code (knip)",
      "command": "bunx knip",
      "hints": [
        "Run `bun run knip` to see full details",
        "Add intentional exports to knip.json entry configuration"
      ]
    },
    {
      "name": "typecheck (tsc)",
      "command": "bunx tsc -p tsconfig.json --noEmit",
      "hints": [
        "Check type definitions in the files you modified",
        "Ensure all imports have proper type declarations"
      ]
    },
    {
      "name": "tests (vitest)",
      "command": "bun run test:coverage",
      "hints": [
        "Run `bun run test` to see detailed test output",
        "Check if new code needs corresponding tests"
      ]
    },
    {
      "name": "tests (bun)",
      "command": "bun test",
      "hints": [
        "Tests must work with both Vitest and Bun runtimes",
        "Avoid using Vitest-specific APIs like vi.stubEnv - use cross-runtime helpers in test-utilities.ts"
      ]
    }
  ]
}
