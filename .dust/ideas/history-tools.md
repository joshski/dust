# History Tools

Tools for traversing commit history to retrieve deleted tasks.

Since tasks are deleted on completion, tooling could rebuild the history of work done, generate indexes, or provide search across historical planning artifacts.
