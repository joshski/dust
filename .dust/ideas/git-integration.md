# Git Integration

Tools for integrating Dust with git workflows.

Branch naming that matches task names. Validation that task branches don't already exist before claiming. Hooks for enforcing commit conventions.
