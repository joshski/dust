# Prompt Generation

Generate prompts for common Dust workflows that can be passed to any AI agent.

Examples include prompts for creating atomic commits, converting ideas to tasks, and validating that documentation matches implementation. Generated prompts would incorporate content from `.dust/goals/` and `.dust/facts/`, ensuring they reflect current project thinking as it evolves over time.
