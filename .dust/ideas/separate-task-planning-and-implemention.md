# Separate task planning and implementation

`dust claude` says: "if the user mentions tasks, run `dust claude tasks`"

But the advice given in that command is all about _authoring_ tasks, not _implementing_ tasks.

Instead the agent should determine if the user wants to add tasks, or implemnent a task - and suggest different commands accordingly.