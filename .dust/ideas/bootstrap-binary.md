# Bootstrap Binary

A published binary tool that can bootstrap Dust in any repository.

A single executable (e.g. downloadable via curl or installed via package managers) that initializes the `.dust/` directory structure with sensible defaults. No runtime dependencies required.

This supports the [easy adoption](../goals/easy-adoption.md) goal by making the first step as simple as possible.
