# dust claude command

Add a `dust claude "prompt"` command that launches Claude, passing all command line arguments to Claude, but also amends the system prompt to tell Claude about dust and how to get started.

This would provide an easy way to start a Claude session that's already aware of dust conventions and can immediately begin working with the dust workflow.
