# dust claude command

Add a `dust claude` command that launches Claude with a system prompt pre-configured for dust workflows.

This would provide an easy way to start a Claude session that's already aware of dust conventions and can immediately begin working with the dust workflow.
