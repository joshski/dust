# Dust next

A `dust next` command could show the next tasks that are not blocked by any other tasks - such that an agent could avoid reading tasks that are blocked - i.e. the tasks that could be worked on next.
