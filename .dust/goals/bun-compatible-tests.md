# Bun-Compatible Tests

Dust's test suite should be compatible with the Bun runtime.

Tests should use standard JavaScript testing patterns that work across both Node.js and Bun. Avoiding runtime-specific test APIs ensures the project can leverage Bun's speed advantages while maintaining compatibility with Node.js.
