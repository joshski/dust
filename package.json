{
  "name": "dust",
  "type": "module",
  "bin": {
    "dust": "./bin/dust"
  },
  "scripts": {
    "lint:tasks": "bun scripts/lint-tasks.ts",
    "validate:links": "bun scripts/validate-links.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6"
  }
}
