/**
 * dust new goal - Goal creation instructions
 *
 * Displays guidance for creating new goals.
 */

import { createTemplateCommand } from '../template-command'

export const newGoal = createTemplateCommand('agent-new-goal')
